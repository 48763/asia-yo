apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment 
spec:
  revisionHistoryLimit: 1
  replicas: 1
  template:
    spec:
      containers:
      - image: image
        name: pod-name
        ports:
        - containerPort: 8080
          name: http
